info:
  project:
    name: jba-msa-payment-order
    version: 1.0.0
server:
  port: 8081
  error:
    include-message: always
spring:
  application:
    name: jba-msa-payment-order
  profiles:
    active: local
  main:
    allow-bean-definition-overriding: on
    lazy-initialization: on
  web-flux:
    base-path: /v1/payment-order
  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd'T'HH:mm:ssZ
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
    r2dbc:
      url: r2dbc:postgresql://db:5432/database
      username: username
      password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      show-sql: true

  datasource:
    initialization-mode: always
    initialize: true
    schema: classpath:/schema.sql
    continue-on-error: true

logging:
  level:
    org.springframework: info
    org.springframework.web: DEBUG
    org.springframework.boot: DEBUG
    com.test.payment.order: DEBUG

test:
  payment-order:
    orders:
      path: /account-receivable-orders